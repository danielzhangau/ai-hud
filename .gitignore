# Build artifacts
*.bin
*.o
*.elf
build/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv*/

# IDE
.vscode/
.idea/

# Claude Code local config + skills (per-machine, not project-wide)
.claude/

# Model binaries
*.rknn
*.onnx
*.pt

# Training data (too large for git)
training/au_speed_dataset/
training/au_speed_dataset.tar.gz
training/datasets/
training/downloads/
training/.wheels/

# 3D model exports (regenerate from .py source)
enclosure/*.stl
enclosure/*.step

# Temporary files
/tmp/

# Empty dirs
sysroot/
dist/

# Raw speed-DB source snapshots (per-fetcher, gz-compressed).
# Re-downloadable from upstream; never commit to keep repo lean.
data/raw/

# Diagnostic / scratch CMake build trees
build-diag/
